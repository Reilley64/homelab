{
  "permissions": {
    "deny": [
      "Read(./.auto.tfvars)",
      "Read(./terraform.tfstate*)",
      "Read(./.terraform/**)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '\\.auto\\.tfvars|terraform\\.tfstate|\\.terraform/' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Holds plaintext secrets. Query Grafana with .grafana-token instead.\"}}' || true"
          }
        ]
      }
    ]
  }
}
